{
  "name": "reeldeal",
  "version": "1.0.0",
  "description": "ReelDeal Movie Discovery App",
  "scripts": {
    "install-all": "cd backend && npm ci --force && cd ../frontend && npm ci --force",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
